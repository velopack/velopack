name: Build

on: 
  push:
    branches: [master, develop]
  pull_request:
    branches: [master, develop]
  workflow_dispatch:

jobs:
  build-rust:
    uses: ./.github/workflows/build-rust.yml

  delay-1: # this delay allows build-rust to secure a priority queue slot for a macos runner (limited to 5 concurrent)
    runs-on: ubuntu-latest
    steps:
      - run: sleep 10

  build-python:
    needs: [delay-1]
    uses: ./.github/workflows/build-python.yml
    
  build-tests:
    needs: [delay-1]
    uses: ./.github/workflows/build-tests.yml
    secrets: inherit # tests need secrets to run

  delay-2: # this delay allows build-python and build-tests to secure queue slots for a macos runner (limited to 5 concurrent)
    runs-on: ubuntu-latest
    steps:
      - run: sleep 30

  build-samples:
    needs: [delay-2]
    uses: ./.github/workflows/build-samples.yml

  build-packages:
    needs: [build-python, build-rust]
    uses: ./.github/workflows/build-packages.yml

  release:
    runs-on: ubuntu-latest
    needs: [build-packages, build-tests, build-samples]
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    steps:
      - name: Invoke Release Workflow
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: release.yml
          inputs: '{ "workflow_run_id": "${{ github.run_id }}" }'