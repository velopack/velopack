[package]
name = "velopack-source-harness"
version = "0.0.0"
edition = "2021"

[dependencies]
velopack = { path = "../../../src/lib-rust", features = ["file-logging"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.5", features = ["derive"] }
